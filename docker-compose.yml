services:
  glm-proxy:
    build: .
    ports:
      - "8082:8082"
    environment:
      - HOST=0.0.0.0
      - PORT=8082
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
